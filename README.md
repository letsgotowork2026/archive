# 취업 및 이직을 위한 모임

## GOAL

### CJH 최준호
- `CKA` 자격증 준비

### LSJ 이수정
- <모던 `리액트` 딥다이브> 정독
- `SQLD` 준비

### PYW 박예원
- `SPRING` 김영한 강의

### LJI 이정인
- `ADsP` 준비 (1월)
- `SQLD` 준비 (2월)

### KSE 김소은

## RULE
1. 개인의 공부는 `일주일`의 공부한 내용을 `PR`로 올린다.
   - 계속 올려도 된다.
2. 그 주의 알고리즘에 대한 문제를 `3개씩` 푼다.
   - 본인이 푼 문제는 간단하게 5분 설명 가능해야 한다.
   - `@최준호` : 문제 3개 셀렉 및 공유
   - `우선순위` 순으로 `TEST01`, `TEST02`, `TEST03` 배치
     - 최소한 `TEST01`은 풀어오자

   - `파이썬` / `C++`로 구현
